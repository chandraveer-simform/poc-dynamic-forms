import React from "react";
import DynamicFormsList from "../../features/DynamicFormsList";

interface Props {}

const Home: React.FC<Props> = (props: Props) => {
  return <DynamicFormsList />;
};
export default Home;
